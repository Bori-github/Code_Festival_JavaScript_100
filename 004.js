// 다음 변수 a를 typeof(a)로 넣었을 때 출력될 값과의 연결이 알맞지 않은 것은?

let a = 1; // number
let b = 2.22; // number
let c = "p"; // string
let d = [1, 2, 3]; //object

console.log(`입력 : ${typeof a}`);
console.log(`입력 : ${typeof b}`);
console.log(`입력 : ${typeof c}`);
console.log(`입력 : ${typeof d}`);
